### 一、 绪论/背景
*   **目的/意义**：
    *   **核心痛点**：视网膜脱离（RD）诊断的紧迫性与黄斑状态（Macula-on/off）对预后的决定性影响。
    *   **技术转型**：说明为何从静态图像转向超声视频——利用**时间维度冗余**克服超声瞬时伪影，捕捉视网膜膜状物的动态波动特征。
*   **相关工作**：
    *   梳理医学影像识别从 **CV 1.0（手工特征）** 到 **CV 2.0（以 U-Net 为核心的空间分割）** 再到 **CV 3.0（3D-CNN/Transformer 时空建模）** 的过程。
    *   提及目前该领域缺乏公开视频基准，而 [ERDES](https://alphaxiv.org/abs/2508.04735) 是首个解决这一缺口的工作。

### 二、 项目安排
*   **任务分工**：
    *   数据工程：超声视频预处理与 ROI 自动提取算法实现。
    *   模型研发：8 种基线模型（3D ResNet, 3D U-Net, Swin-UNETR 等）的迁移与训练。
    *   实验评估：针对 RD 检测与黄斑状态判定的性能测试。
*   **进度安排**：
    *   第一阶段：环境搭建与 ERDES 数据集解析（已完成）。
    *   第二阶段：基线模型训练与两阶段管线跑通（已完成）。
    *   第三阶段：结果对比分析与模型瓶颈挖掘（进行中）。
    *   第四阶段：针对性优化（如可学习采样策略）与论文/报告撰写。

### 三、 方法与实现
*   **总体设计**：
    *   **两阶段级联分诊管线**：
        1.  **Stage 1**：RD 二分类模型，识别是否存在脱离。
        2.  **Stage 2**：黄斑状态判定模型，针对阳性样本区分 Macula-intact 与 Macula-detached。 [Two-stage Pipeline](https://alphaxiv.org/abs/2508.04735v2?page=12)
*   **模块实现**：
    *   **ROI 提取模块**：基于 **YOLOv8**。通过检测眼球主体并裁剪，消除设备文字干扰，减少无效背景计算。
    *   **特征提取骨干网**：实现并对比了卷积类（3D U-Net）与注意力类（ViT/Swin-UNETR）架构。
    *   **时间池化策略**：实现了**选择性池化（Selective Pooling）**，通过设定比例 $r$ 来过滤视频中的非关键帧。 [Pooling Strategy](https://alphaxiv.org/abs/2508.04735v2?page=12)

### 四、 实验与分析
*   **实验设计**：
    *   **数据集**：ERDES 数据集（5,381 个超声片段）。
    *   **超参数**：输入尺寸 $96 \times 128 \times 128$（$D \times H \times W$），AdamW 优化器，学习率 $1.5 \times 10^{-5}$。
*   **实验结果**（直接引用基线数据）：
    *   **RD 检测**：3D U-Net 灵敏度达到 $0.950$，准确率 $0.991$。
    *   **黄斑判定**：3D U-Net 表现最优，准确率为 $0.882$。 [Results](https://alphaxiv.org/abs/2508.04735v2?page=11)
*   **结果分析**：
    *   **瓶颈分析**：模型在 PVD（玻璃体脱离）识别上表现较差，反映了模型对“高动态、低回声”瞬时特征捕捉能力不足。
    *   **采样比例影响**：分析选择性采样比例 $r$ 对性能的影响，证明“全量视频帧”并不等同于“最大信息量”。
*   
### 五、改进
- 针对当前模型的问题进行改进
- 提出方案
- 实验部分看时间

### 六、 结论
*   **总结**：已跑通基于时空特征的视网膜诊断基线，验证了两阶段级联架构在临床分诊中的可行性。
*   **未来方向**：鉴于目前采样比例 $r$ 为人工固定设置，下一步计划引入**可学习的注意力采样机制**，以实现对诊断关键帧的自适应捕获。